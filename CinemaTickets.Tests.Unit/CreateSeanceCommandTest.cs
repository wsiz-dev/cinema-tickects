using CinemaTickets.Domain;
using System;
using System.Collections.Generic;
using System.Text;
using Xunit;

namespace CinemaTickets.Tests.Unit
{
    public class CreateSeanceCommandTest
    {
        //[Fact]
        //public void CreateSeance_WhenItIsPossible_ShouldSuccess()
        //{
        //    using (var sut = new SystemUnderTest())
        //    {
        //        var movie = sut.CreateMovie("Harry Potter", 2001);
        //        sut.MoviesRepository.Add(movie);
        //        var seanceDate = new DateTime(2019, 4, 1, 11, 0, 0);
        //        var quantity = 50;

        //        var command = new CreateSeanceCommand(seanceDate, quantity);
        //        var handler = new CreateSeanceCommandHandler();

        //        handler.Handle(command);

        //        movie = sut.MoviesRepository.GetById(movie.Id);
        //        movie.LoadSeances();

        //        var seance = movie.LoadSeances();

        //    }
        //}
    }
}
